# Session — 2026-06-26 — Methods & infrastructure

> Programme: TRT. A methods-heavy session establishing the programme's operating machinery (following PI-1 Iteration 1, journaled in `2026-06-26-iter1.md`). Executor: thinx-Claude. No approval gates triggered (no Zenodo publish).

## Methods established

- **Tier-0 rename + broadening:** `0-strategy` → `0-program-methods` (VSOK + METHODOLOGY + ROADMAP + `github-workflow.md` agile-devops + `research-practices.md`).
- **Research-console RFC** (PR #2, parked): generalized internal-ops proposal; staged (Phase 1 in-repo journal + Pages; Phase 2 hosted on PeakAI). De-prioritized by JD.
- **CI live** (`.github/workflows/ci.yml`, JD-authorized): traceability acyclic-gate + Lean core typecheck + **math-lint** (no `\operatorname`).
- **Zenodo publishing SOP** (`ops/zenodo-publishing.md`) + paper metadata/RECORD templates + `build-paper.sh`; token handling corrected (`ZENODO_ACCESS_TOKEN_PROD`, mapped at runtime). Token-exposure audit: clean.
- **PI-1 planned** (milestone #1): O1/O2 committed, O3/O4 stretch; issues #3-6.
- **LaTeX render fix** (#7, closed): `\operatorname`→`\mathrm` ×3 in the paper + math-lint guard.
- **Open-problem tracking method:** registry-canonical (`OPN-*` claims, now 7), `build.py` generates `open-problems.md`, GitHub Issues = active subset (`open-problem`/`conf:*`/`gating` labels, `Tracks: OPN-id`). `2-theory/open-problems.md` → pointer. Added ONT-004 + OPN-003..007 (18 claims, acyclic).
- **Figure-placeholder method** (`ops/figures.md`): caption + build-spec placeholders; 5 inserted in the position paper; deposit gate (zero `[FIGURE-PLACEHOLDER]` at v1.0).
- **Session lifecycle:** `research-start.py` (orientation) + `research-wrap.py` (hygiene gate). This wrap: PASS.

## State at wrap

CI green; traceability 18 claims, acyclic; Lean core green; `research-wrap` PASS. Residuals: 5 figure placeholders, references (Aristotle/Russell) remaining — both gate the v1.0 deposit, not this session.

## Carry to next

- **PI-1 Iteration 2:** O1b (finalize Aristotle/Russell refs → cut `TRT-v1.0-public.md` → build PDF → draft Zenodo deposit, JD approves publish); O2b (`Core`→LRT import + flip EXT-001 verified + gate Mathlib build in CI); **O3 kickoff** (finite-graph co-admissibility — gating, critical path).
- Optional: build the 5 figure SVGs (D2) toward a depositable v1.0.
